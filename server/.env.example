# Port the API listens on
PORT=4000

# FavQs API key — sign up at https://favqs.com/api and create one.
# This key must stay server-side only; it is never sent to the mobile app.
FAVQS_API_KEY=your-favqs-api-key
